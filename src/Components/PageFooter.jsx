import { FaPlusCircle } from "react-icons/fa"

const PageFooter = () => {
    return (
        <main   className="PageFooter text-center">
            <FaPlusCircle role="button" className="addButton" />
        </main>
    )
}

export default PageFooter;